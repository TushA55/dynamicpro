name: floyer
description: App that changes profiles based on location

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  awesome_notifications: ^0.6.19
  flutter:
    sdk: flutter

  flutter_colorpicker: ^1.0.3
  flutter_map: ^5.0.0
  flutter_map_location_marker: ^7.0.2
  google_fonts: ^5.1.0
  hive_flutter: ^1.1.0
  latlong2: ^0.9.0
  location: ^5.0.2+1
  page_transition: ^2.0.9
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf